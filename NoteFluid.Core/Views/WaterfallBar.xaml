<UserControl x:Class="NoteFluid.Core.Views.WaterfallBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="100">
    <UserControl.Resources>
        <!-- 瀑布条背景渐变 - 从下到上从亮到暗 -->
        <LinearGradientBrush x:Key="WaterfallGradient" StartPoint="0,1" EndPoint="0,0">
            <GradientStop Color="#FF00BFFF" Offset="0"/>
            <!-- 底部亮蓝色 -->
            <GradientStop Color="#FF1E90FF" Offset="0.3"/>
            <!-- 中部蓝色 -->
            <GradientStop Color="#FF4169E1" Offset="0.6"/>
            <!-- 中上部深蓝 -->
            <GradientStop Color="#00000000" Offset="1"/>
            <!-- 顶部透明 -->
        </LinearGradientBrush>

        <!-- 发光效果 -->
        <DropShadowEffect x:Key="GlowEffect" 
                         Color="#FF00BFFF"
                         BlurRadius="10"
                         ShadowDepth="0"
                         Opacity="0.8"/>
    </UserControl.Resources>

    <Border x:Name="BarBorder"
            Background="{StaticResource WaterfallGradient}"
            BorderBrush="#FF1A1A2E"
            BorderThickness="0"
            CornerRadius="3"
            Effect="{StaticResource GlowEffect}"
            VerticalAlignment="Top"
            ClipToBounds="True">
    </Border>
</UserControl>